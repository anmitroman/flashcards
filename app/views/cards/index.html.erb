<h3>Все карточки флэшкарточкера</h3>

<div class="row">
  <% @card.each do |card| %>
    <div class="col-md-3">
      <%= render partial: "card", locals: { card: card } %>
    </div>
  <% end %>
</div>
